import { useRive, Layout, Fit, Alignment, RuntimeLoader } from "@rive-app/react-canvas";

export type RiveLayout = InstanceType<typeof Layout>;

export { useRive, Layout, Fit, Alignment, RuntimeLoader };
